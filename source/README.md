This directory mirrors the CLI-related source from [`Royaltyprogram/aiops`](https://github.com/Royaltyprogram/aiops).

Current snapshot:

- Source commit: `a9f2436`
- Source version: `0.1.0-beta.2-1-ga9f2436`

Included here:

- `cmd/crux/`
- `scripts/install_local_dev.sh`

The source of truth remains the `aiops` repository. This snapshot is published here so the CLI repo contains both the built bundle and the corresponding source changes.
