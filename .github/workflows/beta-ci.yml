name: Closed Beta CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: tools/codex-runner/package-lock.json

      - name: Install Codex runner dependencies
        run: npm ci --omit=dev
        working-directory: tools/codex-runner

      - name: Run repository verification
        run: make ci-beta

      - name: Start local closed beta server
        run: |
          APP_MODE=local ./output/server > /tmp/agentopt-server.log 2>&1 &
          echo $! > /tmp/agentopt-server.pid

      - name: Wait for liveness
        run: |
          for attempt in $(seq 1 30); do
            if curl -fsS http://127.0.0.1:8082/healthz >/dev/null; then
              exit 0
            fi
            sleep 1
          done
          echo "server did not become healthy" >&2
          exit 1

      - name: Run closed beta smoke
        env:
          BETA_SMOKE_EMAIL: demo@example.com
          BETA_SMOKE_PASSWORD: demo1234
        run: ./scripts/closed_beta_smoke.sh

      - name: Stop local server
        if: always()
        run: |
          if [ -f /tmp/agentopt-server.pid ]; then
            kill "$(cat /tmp/agentopt-server.pid)" || true
          fi

      - name: Dump server log on failure
        if: failure()
        run: cat /tmp/agentopt-server.log
